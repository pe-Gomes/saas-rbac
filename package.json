{
  "name": "saas-rbac",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "db": "prisma studio",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.0.5",
  "engines": {
    "node": ">=18"
  }
}